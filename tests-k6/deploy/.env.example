# Environment variables for DEPLOYED k6 tests.
# --------------------------------------------
# To use, create a file named '.env' in this same directory (tests-k6/deploy/.env)
# and add your production API URL and JWT token.

# The full URL of your deployed backend service on Render.
# IMPORTANT: It must end with /api
API_URL="https://your-backend-service-name.onrender.com/api"

# How to get the token:
# 1. Open your live frontend URL (e.g., https://your-frontend-name.onrender.com).
# 2. Log in with your Google account.
# 3. Open your browser's developer tools (F12).
# 4. Go to the "Application" tab -> "Local Storage" -> click on your site's URL.
# 5. Copy the long string value from the 'token' key.
AUTH_TOKEN="YOUR_DEPLOYMENT_JWT_TOKEN_HERE"